--- 9.98/feq/wrcom.cmn
+++ 10.61/feq/wrcom.cmn
@@ -1,18 +1,19 @@
 C***********************************************************************
       COMMON/WRCOM/ NFOUT, DSS_OUT_COUNT, UNOUT, BRAOUT, NODOUT, ICDOUT,
-     A              DSS_INDEX_OUT, DSS_BUFFER_COLUMN, TIME_STEP_OUT,
-     B              IRR_DT, ACTOUT
+     A              wr_rec, DSS_INDEX_OUT, DSS_BUFFER_COLUMN, 
+     b              TIME_STEP_OUT, IRR_DT, ACTOUT
 
       COMMON/WRCOMC/ NAMOUT
-      COMMON/WRCOMD/ RUNQUAD, OLDF
+      COMMON/WRCOMD/ RUNQUAD, OLDF, quadfactor
 
-      CHARACTER*80 NAMOUT(MNFOUT)
+      CHARACTER*256 NAMOUT(MNFOUT)
       
       INTEGER NFOUT, DSS_OUT_COUNT, IRR_DT
       INTEGER UNOUT(MNFOUT), BRAOUT(MNFOUT), NODOUT(MNFOUT), 
-     A        ACTOUT(MNFOUT), ICDOUT(MNFOUT), DSS_INDEX_OUT(MNFOUT), 
-     B        DSS_BUFFER_COLUMN(MNFOUT), TIME_STEP_OUT(MNFOUT)
+     A        ACTOUT(MNFOUT), ICDOUT(MNFOUT), wr_rec(MNFOUT),
+     b        DSS_INDEX_OUT(MNFOUT), DSS_BUFFER_COLUMN(MNFOUT), 
+     c        TIME_STEP_OUT(MNFOUT)
      
-      REAL*8 RUNQUAD(MNFOUT), OLDF(MNFOUT)
+      REAL*8 RUNQUAD(MNFOUT), OLDF(MNFOUT), quadfactor
 C***********************************************************************
 
