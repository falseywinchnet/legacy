# Python 3.12 is the verified research environment. Component fixture tests
# need only the standard library and do not install these packages.
pefile==2024.8.26
capstone==5.0.7
tree-sitter==0.25.2
tree-sitter-cpp==0.23.4
